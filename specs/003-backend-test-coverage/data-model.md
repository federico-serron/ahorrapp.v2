# Data Model: Cerrar gaps de cobertura de tests en el backend

No aplica — esta feature no agrega, modifica ni elimina ninguna entidad de datos. `User`,
`Transaction` y `Category` se usan tal como ya existen, únicamente como fixtures dentro de los
tests nuevos/reparados. El "contrato" relevante para esta feature no es de datos sino de
comportamiento de funciones — ver `contracts/`.
